# ── Local (query generation, evaluation, data prep) ──────────────────────────
# Install with: pip install -r requirements.txt

torch>=2.0.0
transformers==4.53.0          # pinned — later versions break FlagEmbedding inference
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.21.0
scipy>=1.9.0
scikit-learn>=1.1.0
huggingface_hub>=0.20.0
safetensors>=0.4.0
tqdm>=4.64.0
requests>=2.28.0
google-generativeai>=0.5.0    # Gemini API for query generation

# ── UPPMAX / training cluster ─────────────────────────────────────────────────
# FlagEmbedding must be source-installed (includes bug fix in runner.py):
#   git clone https://github.com/FlagOpen/FlagEmbedding
#   pip install -e ".[finetune]"
#
# Additional on UPPMAX:
#   pip install peft accelerate deepspeed
